t is 5
define f(flag) as:
    if flag == 1:
        local t is 1
    else:
        t is 2
    try:
        print of "hi"
    catch t:
        print of "err"
    return t
print of ("returned " + (str of (f of 0)))
print of ("module t is now " + (str of t))
