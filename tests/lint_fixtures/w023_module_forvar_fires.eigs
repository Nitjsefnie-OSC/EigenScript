t is 5
for t in [1, 2]:
    print of t
define f(flag) as:
    if flag == 1:
        local t is 1
    else:
        t is 2
    return t
print of ("returned " + (str of (f of 0)))
print of ("module t is now " + (str of t))
