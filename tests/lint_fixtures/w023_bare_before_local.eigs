t is 5
define f(flag) as:
    if flag == 1:
        t is 1
        local t is 9
    else:
        local t is 2
    return t
print of ("returned " + (str of (f of 1)))
print of ("module t is now " + (str of t))
